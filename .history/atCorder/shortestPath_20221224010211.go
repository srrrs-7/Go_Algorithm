package atCoder

import "fmt"

func ShortestPath() {
	row := 5
	col := 5

	cnt := createGrid(row, col)
	fmt.Println("shortest path: ", cnt)
}

func createGrid(row, col int) int {
	grid := [][]string{}
	for i := 0; i < row; i++ {
		for i := 0; i < row; i++ {
		}
	}

	return 0
}

func searchPath() {}
